{% extends "base.html.twig" %}
{% block title %}4 Lawn Ave | Deceased Estate{% endblock %}

{% block content %}
  <img src="mockup/img/4lawnave-with-caption.jpg" class="img-fluid" alt="Responsive image">
  <h3 class="m-2">Collections</h3>
  <div class="list-group">
    {% for collection in collections %}
	  <a href="collection.php?c={{ collection.collection|replace({' ': "-", }) }}" class="list-group-item list-group-item-action">{{ collection.collection|title }}</a>
    {% endfor %}
  </div>
{% endblock %}